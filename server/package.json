{
  "name": "react-email-confirm-server",
  "version": "0.0.1",
  "description": "server for react email confirm",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "deploy": "now && now alias",
    "dev": "nodemon server",
    "push": "git add . && git commit -m 'auto push -2-' && git push"
  },
  "author": "jesse heaslip",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "forever": "^0.15.3",
    "mongoose": "^5.9.3",
    "nodemailer": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.4",
    "now": "^13.0.0"
  }
}